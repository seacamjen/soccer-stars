export class Player {
  constructor(public name: string, public dob: string, public club: string, public salary: string, public position: string, public apps: number, public goals: number, public assists: number, public lastApp: string, public image: string) { }
}
